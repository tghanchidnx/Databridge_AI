[pytest]
# Set the root directory for pytest
testpaths = tests

# Add paths to PYTHONPATH for imports
pythonpath = . src tests

# Skip backend integration tests by default (run with: pytest -m backend)
addopts = -m "not backend"

# Custom markers
markers =
    backend: marks tests as requiring NestJS backend (run with: pytest -m backend)
